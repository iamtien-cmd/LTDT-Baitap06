<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="#212121">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to Profile"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#FFF"
        android:layout_gravity="center"
        android:paddingBottom="20dp"/>

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/imageViewProfile"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/user_avatar"
        android:layout_gravity="center"
        android:padding="8dp"/>

    <TextView android:text="Id" style="@style/ProfileLabel"/>
    <TextView android:id="@+id/textViewId" style="@style/ProfileValue"/>

    <TextView android:text="Username" style="@style/ProfileLabel"/>
    <TextView android:id="@+id/textViewUsername" style="@style/ProfileValue"/>

    <TextView android:text="Email" style="@style/ProfileLabel"/>
    <TextView android:id="@+id/textViewEmail" style="@style/ProfileValue"/>

    <TextView android:text="Gender" style="@style/ProfileLabel"/>
    <TextView android:id="@+id/textViewGender" style="@style/ProfileValue"/>

    <Button
        android:id="@+id/btnLogout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="LOGOUT"
        android:textSize="16sp"
        android:textColor="#FFF"
        android:background="#FF3D00"
        android:layout_marginTop="20dp"
        android:padding="12dp"/>
</LinearLayout>
